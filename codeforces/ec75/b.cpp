#include<bits/stdc++.h>
using namespace std;

#define pb push_back
#define mp make_pair
#define rep(i,n)  for (int i = 0; i < n; i++)
#define loop(i,a,n)  for (int i = a; i < n; i++)

void solve(){
    int n;
    cin >> n;
    rep(i,n){
        string st;
        int sum[2];
        cin >> st;
        rep(j,st.length()){
             
        }
    }
}

int main(){

}